@model BeeBilisim.Web.Models.AnasayfaModel
@using System.Security.Claims
@using Microsoft.AspNetCore.Http

@inject IHttpContextAccessor httpContextAccessor
@{
    ViewData["Title"] = "SloganGetir";
    Layout = "~/Views/Shared/_AdminPanelLayout.cshtml";
    string displayName = httpContextAccessor.HttpContext.User.FindFirst(ClaimTypes.Email).Value;

}


<h2> Slogan Bilgisi</h2>
@using (Html.BeginForm("SloganGuncelle", "AdminPanel", FormMethod.Post))
{
    <div class="padding:10px;">
        @Html.HiddenFor(x => x.Id)
        <div>
            <label>Slogan: </label><br />
            @Html.TextBoxFor(q => q.Slogan, new { @class = "form-control" })
        </div>
        <div>
            <label>Slogan Açıklaması: </label><br />
            @Html.TextBoxFor(q => q.SloganAciklama, new { @class = "form-control" })
        </div>
        <div>
            <input type="hidden" name="LastupUser" value="@displayName">
        </div>
        <div style="margin-top:15px;">
            <button class="btn btn-info">Güncelle</button>
        </div>
    </div>
}

