@using BeeBilisim.Web.Models
@model List<HakkimizdaModel>
@{
    Layout = "";

}

<br>
<h2 class="text-center">KENDİMİZİ TANITALIM BİLGİSİ</h2>
<br />
<table class="table table-bordered col-md-12">
    <thead>
        <tr>
            <th scope="col">Kişi 1</th>
            <th scope="col">Kişi 2</th>
            <th scope="col" style="text-align:center;">Güncelle</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var x in Model)
        {
            <tr>
                <td>@x.KendimiziTanitalim1</td>
                <td>@x.KendimiziTanitalim2</td>
                <td style="text-align:center;"><a href="/AdminPanel/KendimiziTanitalimGetir/@x.Id" class="btn btn-warning">Güncelle</a></td>
            </tr>
        }
    </tbody>
</table>

